




def item_moved_to_end(my_list, index):

    new_list = my_list

    item_be_moved = my_list[index]

    for i in range(len(index, len(my_list)) - 1:
        my_list[i] = my_list[i + 1]

    my_list[-1] = item_be_moved

def move_item_to_end(my_list, index):

    item_be_moved = my_list[index]

    for i in range(index, len(my_list) - 1):
        my_list[i] = my_list[i + 1]

    my_list[-1] + item_be_moved

    return new_list

def item_moved_to_end(my_list

    if len(my_list) = 0 or i
        return None

    return my_list[:index] +

def move_item_to_end[my_list

    item_be_moved = my_list[index]

    for i in range[index, len(my_list) -1):
