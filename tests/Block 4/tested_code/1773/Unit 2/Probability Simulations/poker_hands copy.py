def create_deck(cards):
    for rank in range(1,14):
        for suit in range(1,5):
            cards.append([rank, suit])

def

    counter = 0
    

    create_deck(card

    full_house = False

    while not full_house:

    random.shuffle(cards)

    temp_cards = cards

    if cards[0][0] == card0] == cards[1][0] and cards[0][2]
                
